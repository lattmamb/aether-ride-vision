
// This component is kept for reference but is no longer used in the application.
// The main navigation is now handled by the Navbar component in src/components/Navbar.tsx

import React from "react";
import { HoveredLink, Menu, MenuItem, ProductItem } from "@/components/ui/navbar-menu";

export default function NavbarDemo() {
  return null; // Not rendering anything since this component is no longer used
}
